# URL backend (HTTP local hoặc HTTPS production — phải khớp CORS_ORIGIN trên server)
VITE_API_URL=http://localhost:5000

# Giới hạn upload client (MB), nên khớp UPLOAD_MAX_MB trên backend (mặc định 25)
VITE_UPLOAD_MAX_MB=25
